name: KernelSU-Next Build

on:
  push:
  workflow_dispatch:

jobs:
  test:
    name: KernelSU-Next Workflow
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show repository
        run: |
          echo "KernelSU-Next workflow is active"
          ls -la
